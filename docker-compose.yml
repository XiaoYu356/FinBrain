services:
  # mysql:
  #   image: mysql:8
  #   container_name: finbrain-mysql
  #   environment:
  #     MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD:-root123456}
  #     MYSQL_DATABASE: finbrain
  #     TZ: Asia/Shanghai
  #   ports:
  #     - "3306:3306"
  #   volumes:
  #     - mysql_data:/var/lib/mysql
  #     - ./init.sql:/docker-entrypoint-initdb.d/init.sql
  #   healthcheck:
  #     test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
  #     interval: 10s
  #     timeout: 5s
  #     retries: 5
  #   networks:
  #     - finbrain-network

  redis:
    image: redis:7-alpine
    container_name: finbrain-redis
    ports:
      - "6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - finbrain-network

  etcd:
    image: quay.io/coreos/etcd:v3.5.5
    container_name: finbrain-etcd
    environment:
      ETCD_AUTO_COMPACTION_MODE: revision
      ETCD_AUTO_COMPACTION_RETENTION: "1000"
      ETCD_QUOTA_BACKEND_BYTES: "4294967296"
      ETCD_SNAPSHOT_COUNT: "50000"
    command: etcd -advertise-client-urls=http://127.0.0.1:2379 -listen-client-urls http://0.0.0.0:2379 --data-dir /etcd
    healthcheck:
      test: ["CMD", "etcdctl", "endpoint", "health"]
      interval: 30s
      timeout: 20s
      retries: 3
    networks:
      - finbrain-network

  minio:
    image: minio/minio:RELEASE.2023-03-20T20-16-18Z
    container_name: finbrain-minio
    environment:
      MINIO_ACCESS_KEY: minioadmin
      MINIO_SECRET_KEY: minioadmin
    ports:
      - "9000:9000"
      - "9001:9001"
    command: server /data --console-address ":9001"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9000/minio/health/live"]
      interval: 30s
      timeout: 20s
      retries: 3
    networks:
      - finbrain-network

  milvus:
    image: milvusdb/milvus:v2.4.0
    container_name: finbrain-milvus
    command: ["milvus", "run", "standalone"]
    environment:
      ETCD_ENDPOINTS: etcd:2379
      MINIO_ADDRESS: minio:9000
    volumes:
      - milvus_data:/var/lib/milvus
    ports:
      - "19530:19530"
      - "9091:9091"
    depends_on:
      etcd:
        condition: service_healthy
      minio:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9091/healthz"]
      interval: 30s
      timeout: 20s
      retries: 3
    networks:
      - finbrain-network

  # java-backend:
  #   build:
  #     context: ./finbrain-java
  #     dockerfile: Dockerfile
  #   container_name: finbrain-java
  #   environment:
  #     DB_HOST: mysql
  #     DB_PORT: 3306
  #     DB_NAME: finbrain
  #     DB_USERNAME: root
  #     DB_PASSWORD: ${MYSQL_ROOT_PASSWORD:-root123456}
  #     REDIS_HOST: redis
  #     REDIS_PORT: 6379
  #     JWT_SECRET: ${JWT_SECRET:-finbrain-jwt-secret-key-must-be-at-least-256-bits-long}
  #   ports:
  #     - "8080:8080"
  #   depends_on:
  #     mysql:
  #       condition: service_healthy
  #     redis:
  #       condition: service_healthy
  #   healthcheck:
  #     test: ["CMD", "curl", "-f", "http://localhost:8080/actuator/health"]
  #     interval: 30s
  #     timeout: 10s
  #     retries: 3
  #   networks:
  #     - finbrain-network

  # python-ai:
  #   build:
  #     context: ./finbrain-ai
  #     dockerfile: Dockerfile
  #   container_name: finbrain-ai
  #   environment:
  #     DASHSCOPE_API_KEY: ${DASHSCOPE_API_KEY}
  #     LLM_MODEL: qwen-max
  #     MILVUS_HOST: milvus
  #     MILVUS_PORT: 19530
  #     JAVA_API_BASE: http://java-backend:8080
  #     REDIS_HOST: redis
  #     REDIS_PORT: 6379
  #   ports:
  #     - "8001:8001"
  #   depends_on:
  #     milvus:
  #       condition: service_healthy
  #     java-backend:
  #       condition: service_healthy
  #   networks:
  #     - finbrain-network

  # frontend:
  #   build:
  #     context: ./finbrain-vue
  #     dockerfile: Dockerfile
  #   container_name: finbrain-frontend
  #   ports:
  #     - "80:80"
  #   depends_on:
  #     - java-backend
  #     - python-ai
  #   networks:
  #     - finbrain-network

networks:
  finbrain-network:
    driver: bridge

volumes:
  mysql_data:
  milvus_data:
